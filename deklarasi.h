#ifndef DEKLARASI_H
#define DEKLARASI_H
#include <string>

void suitJawa(std::string user, std::string computer);
void tebakAngka(std::string user, std::string computer);
int angkaRand(int rentang);
int point(int point, char keterangan);

#endif